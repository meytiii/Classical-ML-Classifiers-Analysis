# تحلیل عمیق و مقایسه طبقه‌بندهای کلاسیک یادگیری ماشین 🧠
![main](main.png)
این مخزن (Repository) شامل پیاده‌سازی، آزمایش و تحلیل جامع ۷ الگوریتم مشهور یادگیری ماشین (Classic ML Classifiers) است. هدف اصلی این پروژه، بررسی رفتار این مدل‌ها در مواجهه با چالش‌های واقعی داده کاوی مانند **ابعاد بالا**، **نویز** و **ساختارهای غیرخطی** است.

---

## 📊 درباره پروژه

در این پژوهش، ما صرفاً به اجرای مدل‌ها اکتفا نکرده‌ایم؛ بلکه با طراحی یک آزمایش کنترل‌شده (Experimental Design)، اثرات پیش‌پردازش، تنظیم فراپارامترها (Hyperparameter Tuning) و موازنه بایاس-واریانس را بررسی کرده‌ایم.

### مدل‌های پیاده‌سازی شده:
*   K-Nearest Neighbors (kNN)
*   Support Vector Machines (SVM) با کرنل‌های Linear, RBF, Poly
*   Logistic Regression
*   Linear Classifier (SGD)
*   Decision Tree
*   Naive Bayes
*   Multi-Layer Perceptron (MLP Neural Network)

---

## 📂 مجموعه داده‌ها (Datasets)

برای به چالش کشیدن مدل‌ها، سه سناریوی مختلف طراحی شده است:

1.  **چالش ابعاد بالا (High-Dimensionality):** استفاده از دیتاست `Digits` (تصاویر ارقام) که دارای ۶۴ ویژگی پیکسلی است.
2.  **چالش نویز و پایداری (Robustness):** استفاده از دیتاست `Breast Cancer` که به صورت مصنوعی و کنترل‌شده به آن نویز گاوسی (Gaussian Noise) اضافه شده است.
3.  **چالش غیرخطی بودن (Non-Linearity):** استفاده از دیتاست مصنوعی `Make Moons` که دارای مرز تصمیم هلالی و پیچیده است.

---

## 📈 نتایج و تحلیل

ما برای ارزیابی دقیق، از روش **5-Fold Cross-Validation** و **Grid Search** استفاده کردیم تا بهترین پارامترها برای هر مدل انتخاب شود.

### نمونه خروجی (مرز تصمیم در داده‌های غیرخطی)
تصویر زیر تفاوت عملکرد مدل‌های خطی (که در تفکیک داده‌های پیچیده ناتوان‌اند) و مدل‌های غیرخطی مثل SVM و kNN را نشان می‌دهد:

![results](results.png)
*(توجه: تصویر بالا خروجی کد نوت‌بوک است که مرزهای تصمیم‌گیری را روی دیتاست Moons نشان می‌دهد.)*

### یافته‌های کلیدی:
*   **SVM** پایدارترین عملکرد را در بین تمام مدل‌ها داشت و هم در ابعاد بالا و هم در برابر نویز مقاومت خوبی نشان داد.
*   **مدل‌های خطی** (مانند Logistic Regression) در دیتاست‌های غیرخطی دچار خطای بایاس (Underfitting) شدند.
*   **MLP** روی داده‌های نویزی دچار بیش‌برازش (Overfitting) شد که نیاز به تنظیم دقیق‌تر (Regularization) دارد.

---

## 🛠 نحوه اجرا

این پروژه در قالب **Jupyter Notebook** نوشته شده است و به راحتی در Google Colab یا سیستم شخصی قابل اجراست.

### پیش‌نیازها
برای اجرای کدها به کتابخانه‌های زیر نیاز دارید:

```python
pip install numpy pandas matplotlib seaborn scikit-learn
